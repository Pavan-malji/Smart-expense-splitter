VITE_API_URL=http://localhost:5000/api
```

That's it! Just one line for now.

---

## **Important Notes:**

### **⚠️ File Must Start with a Dot**
- ✅ `.env` (correct)
- ❌ `env` (wrong - missing dot)
- ❌ `.env.txt` (wrong - has extension)

### **🔒 Add to .gitignore**

Create/update `.gitignore` file in the `frontend` folder:
```
node_modules
dist
.env
.env.local
```

This prevents your `.env` from being uploaded to GitHub (important for security!)

---

## **File Structure Should Look Like:**
```
frontend/
├── node_modules/
├── public/
├── src/
├── .env          ← Your new file
├── .gitignore    ← Add .env to this
├── index.html
├── package.json
└── vite.config.js